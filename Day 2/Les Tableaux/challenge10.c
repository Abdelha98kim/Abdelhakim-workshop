#include <stdio.h>

int main(){
    int la_taille_de_table;
    printf("Saisez le nombre des elements que tu veux ajouter aux tableaux: ");
    scanf("%d", &la_taille_de_table);
    int tableux[la_taille_de_table];

    for (int i = 0; i < la_taille_de_table; i++) {

    return 0;
}